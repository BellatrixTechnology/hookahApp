// import {Colors} from '../../../../traderr-rn/src/config/Theme';
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  verticalLine: {
    flex:1,
    width: 1,
    // backgroundColor: Colors.Border,
  },
});

export default styles;
